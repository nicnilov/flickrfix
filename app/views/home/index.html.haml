-if current_flickr_account.present?
  %p design albums grid
-else
  .center-block
    .container
      .row
        .col-lg-12
          %a.btn.btn-default{id: 'add-flickr-account', :href => "#"}
            %span Connect Flickr Account
